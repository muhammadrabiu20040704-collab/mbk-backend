export enum MessageType {
  TEXT = "text",
  IMAGE = "image",
  VIDEO = "video",
}

export enum MessageStatus {
  SENT = "sent",
  DELIVERED = "delivered",
  READ = "read",
}

export enum ConversationType {
  DIRECT = "direct",
}
